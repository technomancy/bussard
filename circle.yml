dependencies:
  pre:
    - sudo apt-get update -qq > /dev/null && sudo apt-get install -y luarocks love > /dev/null
    - sudo luarocks install luacheck > /dev/null && sudo luarocks install lunatest > /dev/null

test:
  override:
    - make ci
