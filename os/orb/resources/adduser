 -- -*- lua -*-

local _env, args = ...

if(#args == 2) then
   orb.add_user(args[1], args[2])
else
   print("Add a new user. Requires sudo privileges.\n")
   print("Usage:")
   print("  adduser USER PASSWORD")
end
