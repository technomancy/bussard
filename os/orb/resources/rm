-- -*- lua -*-

local env, args = ...

if(args[1] == "--help") then
   print("Remove files.\n")
   print("Usage:")
   print("  rm FILE1 [FILE2...]")
else
   for _,filename in pairs(args) do
      io.rm(filename, env.CWD)
   end
end
